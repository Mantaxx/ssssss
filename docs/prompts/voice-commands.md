# polecenia głosowe dla mapy i warstw

**Prompt:**

```text
Zintegruj hook `useSpeechRecognition` z biblioteki `react-speech-recognition` z komponentem React. Zdefiniuj gramatykę poleceń rozpoznającą wzorce: "pokaż [warstwa] na [wysokość]" i "ustaw czas na [data/godzina]". Po rozpoznaniu polecenia wywołaj callback aktualizujący stan aplikacji (wybór warstwy, wysokości i czasu) na mapie Mapbox. Zaimplementuj obsługę potwierdzeń głosowych (SpeechSynthesis) z krótkimi komunikatami po polsku.
```
